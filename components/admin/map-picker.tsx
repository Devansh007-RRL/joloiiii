"use client";

// This file is no longer in use and is intentionally left blank to resolve a build error.
// It previously imported Leaflet CSS, which caused a build failure after the library was removed.
export default function MapPicker() {
  return null;
}
