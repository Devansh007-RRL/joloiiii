import React from 'react';

// This is a pass-through layout required by Next.js for this route segment.
// It is being kept inert to prevent routing conflicts.
export default function EmployeeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
